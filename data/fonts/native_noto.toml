# The Native Font Theme provides optimum text rendering on every device and OS.
# Note that the native font families are automatically populated by Academic.

# Font style metadata
name = "native_noto"

# Optional Google font URL
google_fonts = ""

# Custom CSS fonts
# Font families
heading_font = ["Noto Sans SC", "sans-serif"]
body_font = ["Noto Serif SC", "serif"]
nav_font = ["Noto Sans SC", "sans-serif"]
mono_font = ["Source Code Pro", "Roboto Slab"]